/* exported setValue */
// define function setValue with 3 parameters object, key, value
// do not have a return statement
// push into the object at key the value
function setValue(object, key, value) {
  object[key] = value;
}
